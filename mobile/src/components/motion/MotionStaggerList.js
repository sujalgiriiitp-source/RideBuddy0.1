import React from 'react';
import { View } from 'react-native';

export const MotionStaggerList = ({ children }) => <View>{children}</View>;
export const MotionStaggerItem = ({ children }) => <View>{children}</View>;

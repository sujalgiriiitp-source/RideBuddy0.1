{
  "name": "ridebuddy-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "node server/index.js",
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix mobile install && npm --prefix mobile run build",
    "postinstall": "npm --prefix server install --omit=dev"
  },
  "engines": {
    "node": ">=18"
  }
}
